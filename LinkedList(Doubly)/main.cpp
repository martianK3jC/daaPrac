#include <iostream>
#include "linkedlist.h"
using namespace std;

int main(){
	List* list = new LinkedList();
	char ch;
	int num, pos, res;
	
	do{
		try{
			cout<<"Option: ";
			cin>>ch;
			switch(ch){
				case 'a':
					cin>>num;
					
			}
		}
	}
}